const seccionPrincipal = document.querySelector(".seccion-principal")
const contenidoPrincipal = document.querySelector(".contenido-principal")

console.log(seccion-principal)
console.log(contenido-principal)

seccionPrincipal.innerHTML= `
 <div class="contenido-principal">
        <h1>${nombreComic}<h1>
        <p>${sinopsis}</p>
        <a class="boton" href="#">Ver el Cómic</a>
      </div>
      <div class="imagen-principal" style="background-image:url('img/imagen.jpg')"
`
comic.escenas.forEach8(escenas => {
  const miCard = document.querySelector
}

)
